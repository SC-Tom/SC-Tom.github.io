module.exports = (context) => {
  return {
    'ClassDeclaration': (node) => { // eslint-disable-line
      if (node.superClass) {
        context.report(node, 'Class extend is not supported.', {});
      }
    }
  };
};
