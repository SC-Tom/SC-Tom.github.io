module.exports = (context) => {
  return {
    'Property': (node) => { // eslint-disable-line
      if (node.type === 'Property') {
        if (node.kind === 'set' || node.kind === 'get') {
          context.report(node, 'getters/setters are not allowed.', {});
        }
      }
    }
  };
};
