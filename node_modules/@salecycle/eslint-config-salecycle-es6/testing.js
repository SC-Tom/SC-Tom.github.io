const merge = require('lodash.merge');
const backend = require('./backend');

module.exports = merge({}, backend, {
  env: {
    node: true,
    browser: true,
    mocha: true
  },
  rules : {
    'no-unused-vars' : 0,
    'no-console': 1,
    'global-require': 0,
    "max-len": 0,
    "no-unused-expressions": 0,
    'no-var': 0,
    "class-methods-use-this": 0,
    "sort-imports": 0, // for now
    "sort-vars": [0, {"ignoreCase": true}], // for now
    "sort-keys": 0 // for now
  },
  globals: {
    'APP_CONFIG': true,
    describe: true,
    it: true,
    beforeEach: true,
    afterEach: true,
    browser: true,
    sinon: true,
    expect: true,
    before: true,
    after: true,
    xit: true
  }
});
